import { toCapitalize } from '../../../helpers/to-capitalize';
import { module, test } from 'qunit';

module('Unit | Helper | to capitalize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toCapitalize(42);
  assert.ok(result);
});
